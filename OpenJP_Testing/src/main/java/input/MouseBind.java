package input;

public class MouseBind extends KeyboardBind {

    /**
     * Create a new mouse binding
     * @param keycode GLFW mouse button code (<code>GLFW.GLFW_MOUSE_*</code>)
     */
    public MouseBind(int keycode) {
        super(keycode);
    }
    
}
